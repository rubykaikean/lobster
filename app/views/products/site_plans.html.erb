<a href="<%= reservation_path(@product) %>"><button class="btn btn-sm btn-primary">Back</button></a>

<div class="row" id="content_div">
  <div class="col-lg-12">
	<h3 class="text-thin">Site Plan</h3>
	<div class="jumbotron">
	  <% @plans.each do |plan| %>
		<%= image_tag(plan.image.url, class: "img-responsive thumbnail") %>
		<% end %>
	</div>
	</div>
</div>