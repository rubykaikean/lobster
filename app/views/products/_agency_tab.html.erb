<div id="agency_assign-tab" class="tab-pane fade">
	<div class="panel">
    <div class="panel-body">
      <%#= form_tag update_agency_linkage_product_path(@agency), :class => "panel-body form-horizontal form-padding" do %>

			  <div class="form-group pad-ver">
		      <label class="col-md-3 control-label">Agency</label>
		      <div class="col-md-9">
		          <!-- Inline radio buttons -->
		        <select class="selectpicker" name="product[type_id]">
							<option>Select An Option</option>
							<%# Company.where("type_id = 2").each do |p| %>
								<option value="<%#= t.id %>" ><%#= p.name %></option>
							<%# end %>
						</select>
		      </div>
		    </div>

      <%# end %>
    </div>
  </div>
</div>