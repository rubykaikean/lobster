<%= form_for(@sources_type) do |f| %>
  <% if @sources_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sources_type.errors.count, "error") %> prohibited this sources_type from being saved:</h2>

      <ul>
      <% @sources_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
